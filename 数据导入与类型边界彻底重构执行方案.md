# 数据导入与类型边界渐进修复方案

> 适用项目：ProPoints / uni-app x / UTS Android
> 编写日期：2026-05-26
> 调整日期：2026-05-26
> 背景：数据导入近期连续暴露 `UTSJSONObject`、`UTSObject`、强类型 DTO、本地存储 JSON 之间的运行时转换问题。本文不再主张一次性“彻底重构”，而是调整为小步、可验证、兼容逻辑集中管理的渐进修复方案。

---

## 1. 结论摘要

当前问题不是单个导入 bug，而是 **动态数据边界设计不统一**：

```text
外部 JSON / 本地 Storage JSON / UTSJSONObject
  -> 直接 as 强类型对象或强类型数组
  -> Android UTS 运行时 ClassCastException / ArrayStoreException / NullPointerException
```

修复方向应统一为：

```text
动态数据只允许停留在边界层
边界层负责 parse / structural validate / typed build / semantic validate
业务层、页面层、Store 层只消费强类型对象或显式失败结果
```

但执行策略必须收敛：

```text
先止血 Transfer 导入崩溃
再整理 TransferService 内部边界
最后按真实触发频率逐步扩展到 storage/session/snapshot 等边界
```

该设计可以作为项目级规则复用，但不应一次性全项目铺开。优先级如下：

- P0：`TransferService.unseal()` 与导入 preview/apply，解决外部 JSON 崩溃、缺字段、重复数据、半写入。
- P1：导入链路会触发的本地历史兼容，例如 roster conflict、round/snapshot 读取。
- P2：`session-adapter.uts`、`storage-repository.uts`、`default-data-parser.uts`、`AnQuSnapshotService.uts`、`StageCoefService.uts` 等其他动态边界，按模块逐步改。

---

## 2. 已确认的同类问题模式

### 2.1 已经发生过的运行时问题

| 场景                   | 问题写法                               | 运行时表现                                           | 根因                                       |
| ---------------------- | -------------------------------------- | ---------------------------------------------------- | ------------------------------------------ |
| TransferService.unseal | `payload as TransferPayload`           | `UTSJSONObject cannot be cast to UTSObject`          | JSON 解析对象不是真实 typed payload        |
| AppStore preview/apply | `payload as UTSJSONObject`             | `ConfigPayload cannot be cast to UTSJSONObject`      | 强类型 payload 不能反向强转 JSON           |
| unseal catch           | `(e as UTSJSONObject)['message']`      | `ClassCastException cannot be cast to UTSJSONObject` | Java/Kotlin exception 不是 UTSJSONObject   |
| checkRosterConflict    | `local['skill_level'] as number`       | `null cannot be cast to non-null type kotlin.Number` | 本地历史数据字段缺失                       |
| session-adapter        | `worker as SessionWorkerStorageData`   | `SessionBinStorageWorkerData cannot be cast...`      | UTS Android 是名义类型，结构相似也不能强转 |
| session-adapter 数组   | 把 `UTSJSONObject` 放入 typed DTO 数组 | `ArrayStoreException`                                | typed 数组必须放真实 typed object literal  |

### 2.2 当前仍存在的相似边界

扫描结果显示，项目里仍有大量边界读取：

#### TransferService.uts

- `validateAnQuPayload()`、`validateConfigPayload()` 内部大量：
  - `as any[]`
  - `as UTSJSONObject`
- `buildAnQuPayloadFromJSON()` / `buildConfigPayloadFromJSON()` 依赖前置校验完全覆盖字段。
- 当前高风险点：`validateAnQuPayload()` 校验了 `bins[].id`、`bins[].koji`，但 builder 还读取 `bins[].seq`、`bins[].floor`。

#### session-adapter.uts

已经有较好的边界雏形：

- `parseSessionWorkerStorageData()`
- `parsePositionedBinStorageWorkerData()`
- `parseDuiQuStorageWorkerData()`
- `buildSessionBinStorageWorkerData()`
- `buildDuiQuStorageWorkerData()`

但仍存在局部直接读取：

```ts
const workersRaw = binObj["workers"] as UTSJSONObject[] | null;
const binsRaw = raw["bins"] as UTSJSONObject[] | null;
```

这类读取如果字段为非数组，仍可能引出风险。

#### storage-repository.uts

索引提取函数中存在：

```ts
const bins = binsRaw as UTSJSONObject[];
const workers = bins[i]["workers"] as UTSJSONObject[] | null;
const personId = workers[i]["person_id"] as number | null;
```

这属于本地 Storage JSON 边界，应改为安全 reader 或集中 adapter。

#### default-data-parser.uts

存在：

```ts
raw["WHEAT_MATERIAL"] as UTSJSONObject[] | null;
payload["workers"] as UTSJSONObject[] | null;
```

这也是动态默认值解析边界。

#### AnQuSnapshotService.uts

存在：

```ts
const bins = data["bins"] as UTSJSONObject[];
```

导入快照来自本地持久化，也应使用统一边界读取。

#### StageCoefService.uts

存在：

```ts
const stages = snapshot["stages"] as UTSJSONObject | null;
const liangTang = snapshot["liangTang"] as UTSJSONObject | null;
```

系数快照也应纳入同一套“快照 DTO 解析边界”。

---

## 3. 目标架构

### 3.1 总原则

1. **raw JSON 不跨服务边界**
   `UTSJSONObject` 只能存在于 parser / adapter / repository 边界内部。

2. **禁止 JSON 直接 as 业务强类型**
   禁止：

   ```ts
   const payload = raw as TransferPayload;
   const rounds = rawRounds as FermentRound[];
   const workers = rawWorkers as SessionWorkerStorageData[];
   ```

3. **强类型对象必须逐字段构造**
   允许：

   ```ts
   return {
     id: readNumber(raw, "id"),
     name: readString(raw, "name"),
   } as PersonData;
   ```

4. **动态数组必须先验证或过滤，再逐项构造**
   禁止：

   ```ts
   const bins = raw["bins"] as UTSJSONObject[];
   ```

   推荐：

   ```ts
   const rawBins = readObjectArray(raw, 'bins')
   for (...) {
     bins.push(buildBinFromJSON(rawBins[i]))
   }
   ```

5. **类型校验和业务语义校验分层**

   ```text
   structural validation：字段存在、类型正确
   semantic validation：空包、重复、业务冲突、轮次匹配
   ```

6. **页面和 Store 不处理 raw JSON**
   页面只处理 `ImportPreview` / `ImportResult`。

---

## 4. 数据导入渐进修复目标链路

### 4.1 当前链路问题

当前导入链路大致为：

```text
transfer-import.uvue
  -> AppStore.previewTransferImport(envelope)
    -> TransferService.unseal(text)
      -> JSON.parseObject(text)
      -> validateXXXPayload(raw)
      -> buildXXXPayloadFromJSON(raw)
    -> validateClassMatch
    -> resolveRound
    -> detectImportConflicts
  -> AppStore.applyTransferImport(payload, resolvedRoundId, applyFloorConfig)
```

主要问题：

- `unseal()` 同时做 parse、结构校验、typed build、部分业务判断。
- `validateXXXPayload()` 和 `buildXXXPayloadFromJSON()` 强耦合，validate 漏字段会导致 builder 崩。
- `previewTransferImport()` 对 `AMBIGUOUS` 轮次直接 `success=false`，但页面存在候选选择 UI，交互不一致。
- 空包、重复数据、语义冲突未形成统一校验层。

### 4.2 渐进修复后链路

P0 阶段不新增 Facade/Builder 目录，先保持外部调用不变，只收紧 `TransferService.unseal()` 的内部边界：

```text
transfer-import.uvue
  -> AppStore.previewTransferImport(envelope)
    -> TransferService.unseal(text)
      -> parseTransferText(text)
      -> validateTransferEnvelopeShape(raw)
      -> validateAnQuShape(raw) / validateConfigShape(raw)
      -> buildAnQuPayload(raw) / buildConfigPayload(raw)
      -> validateTransferPayloadSemantics(payload)
    -> validateClassMatch
    -> resolveRound
    -> detectImportConflicts
  -> AppStore.applyTransferImport(payload, resolvedRoundId, applyFloorConfig)
```

P2 以后如果 `AppStore` 继续膨胀，才考虑进一步演进为：

```text
TransferImportFacade.preview(text, context)
TransferImportFacade.apply(preview, options)
```

### 4.3 文件组织建议

短期建议：

```text
utils/json-reader.uts              # 只补安全读取工具
domain/services/TransferService.uts # 内部拆函数，外部签名不变
domain/stores/AppStore.uts          # 只做必要 preview/apply 修正
```

暂缓新增以下目录，避免一次性产生过多抽象和兼容层：

```text
domain/services/transfer-import/
  TransferImportFacade.uts
  TransferPayloadParser.uts
  TransferPayloadDecoder.uts
  TransferPayloadShapeValidator.uts
  TransferPayloadSemanticValidator.uts
  TransferImportPreviewBuilder.uts
  TransferImportApplyService.uts
  TransferImportErrors.uts
```

只有当 `TransferService.uts` 内部拆分完成后仍明显臃肿，且已有稳定测试样例覆盖 CONFIG / ANQ 导入，再按“一个文件解决一个明确问题”的方式逐步提取。

---

## 5. 关键类型设计

### 5.1 通用 Result 类型

建议增加轻量结果类型，避免通过 `throw` 表示业务失败。

```ts
type DecodeResult<T> = {
  success: boolean;
  value: T | null;
  error: TransferError | null;
};
```

说明：项目已有经验指出 UTS 原生层不推荐随意 `throw new Error()`，因此业务错误应返回结果对象。

### 5.2 ImportApplyOptions

```ts
type ImportApplyOptions = {
  resolvedRoundId: number | null;
  applyFloorConfig: boolean;
  confirmedWarnings: boolean;
  confirmedConflicts: boolean;
};
```

### 5.3 Raw 不外泄约束

`TransferPayloadDecoder` 的唯一输出应为：

```ts
TransferPayload;
```

而不是：

```ts
UTSJSONObject;
UTSJSONObject | TransferPayload;
any;
```

---

## 6. Transfer 导入分阶段执行方案

### Phase 0：冻结边界规则与补齐工具

#### 目标

形成项目统一规范，不再继续引入新的裸 `as UTSJSONObject[]` 风险。

#### 执行项

1. 扩展 `utils/json-reader.uts`：

   ```ts
   readObjectArray(obj, key): UTSJSONObject[]
   readStringArray(obj, key): string[]
   readNumberArray(obj, key): number[]
   isNumber(value): boolean
   isString(value): boolean
   isObjectArray(value): boolean
   ```

2. 明确两类 reader：

   - 容错读取：用于本地历史数据、索引提取。
   - 严格校验：用于导入 JSON、外部输入。

3. 建立代码规则：

   ```text
   外部 JSON / Storage JSON -> 只能在 *Parser / *Adapter / *Decoder 文件中直接访问 UTSJSONObject。
   Service / Store / Page 不新增 raw cast。
   ```

#### 验收

- 新增工具通过 Android 编译。
- 文档规则加入本方案，后续 PR 以此检查。

---

### Phase 1：先做 Transfer 导入止血增强

#### 目标

不大改 UI 的前提下，先解决最容易继续出崩溃或脏数据的问题。

#### 执行项

1. 补齐 ANQ shape 校验：

   ```text
   bins[].id: string
   bins[].seq: number
   bins[].floor: number
   bins[].koji: number
   ```

2. 拒绝空 ANQ：

   ```text
   bins.length == 0 -> EMPTY_BINS
   ```

3. 拒绝空 CONFIG：

   ```text
   persons == null && coef == null -> INVALID_PAYLOAD
   ```

4. 检测 CONFIG 重复：

   ```text
   duplicate person.id
   duplicate non-empty person.name
   duplicate coef.stages.code
   duplicate coef.duiQu.key
   duplicate coef.liangTang.key
   ```

5. 检测 ANQ 重复：

   ```text
   duplicate bin.id
   duplicate floor + seq
   same key but different koji -> blocking error
   duplicate f.floorNo
   ```

6. 修正 ambiguous round 行为：

   推荐改为：

   ```text
   success=true
   matchStatus=AMBIGUOUS
   candidates=候选轮次
   canImport=false，直到用户选择候选
   ```

   这样页面已有候选选择逻辑可以真正生效。

#### 验收

- 构造非法 JSON 手工测试不会崩溃，只返回错误。
- Android 编译通过。

---

### Phase 2：拆分 TransferService.unseal

#### 目标

让 `unseal()` 不再成为“万能函数”。

#### 拆分建议

```ts
parseTransferText(text): DecodeResult<UTSJSONObject>
readPayloadKind(raw): DecodeResult<PayloadKind>
validateTransferBaseShape(raw): TransferError | null
validateAnQuShape(raw): TransferError | null
validateConfigShape(raw): TransferError | null
buildAnQuPayload(raw): AnQuPayload
buildConfigPayload(raw): ConfigPayload
validateAnQuSemantics(payload): TransferError | null
validateConfigSemantics(payload): TransferError | null
decodeTransferPayload(text): UnsealResult
```

其中 `decodeTransferPayload()` 可以暂时保持原 `unseal()` 对外签名，降低调用方改动：

```ts
export function unseal(text: string): UnsealResult {
  return decodeTransferPayload(text);
}
```

#### 验收

- `AppStore.previewTransferImport()` 无需感知内部拆分。
- 现有导出生成的 CONFIG / ANQ 均可正常 preview。
- Android 编译通过。

---

### Phase 3：后置评估 PreviewBuilder 与 ApplyService

#### 目标

Phase 3 不作为 P0 必做项，只在 P0/P1 稳定后评估是否需要把 AppStore 的导入编排拆出。

#### 为什么后置

`AppStore.previewTransferImport()` 当前确实同时负责：

- unseal
- 班次校验
- 轮次解析
- 冲突检测
- canImport 推导
- requiresConfirmation 推导

但这些职责目前仍围绕同一条导入链路。若在止血阶段同时新增 `TransferImportFacade`、`TransferImportPreviewBuilder`、`TransferImportApplyService`，会增加文件数量、调用层级和历史兼容入口，反而容易制造新的长期遗留。

#### 触发提取的条件

只有满足以下任一条件才提取：

```text
AppStore 导入相关代码继续明显膨胀
preview/apply 被第二个入口复用
TransferService 内部拆分后仍难以维护
已有 CONFIG / ANQ 回归样例可以保护行为不变
```

#### 后续可能的新边界

```text
AppStore
  -> load current app context
  -> TransferImportFacade.preview(text, context)
  -> return ImportPreview
```

#### 验收

- 不改变页面行为。
- 不引入第二套 preview/apply 结果结构。
- 新旧入口不并存；如果提取，旧逻辑必须删除或变成薄代理。

---

### Phase 4：Apply 写入事务化与回滚策略

#### 问题

当前 CONFIG apply 中：

```text
先保存 persons
再保存 coef
如果 coef 失败，persons 可能已经写入
```

这会产生部分写入。

#### 方案

在本地 key-value 场景下，至少做到“预检查优先”：

1. apply 前先检查 activeCoefSet 是否存在。
2. 先构造所有待写入数据。
3. 所有构造成功后再执行保存。
4. 返回 result 时明确哪些写入成功。

如需更严格，可以实现轻量 staged write：

```text
read old values
write new values
失败时尝试恢复 old values
```

但恢复写入需要谨慎，优先避免部分写入。

#### 验收

- CONFIG 导入不会出现人员已覆盖但系数失败的半导入状态。
- ANQ 快照保存失败时不写楼层配置，或明确调整顺序避免半导入。

---

## 7. 是否要在其他地方共用该边界设计？

结论：**规则需要共用，代码不急于共用。**

原因：本项目是 local-first，核心数据大量来自：

```text
uni storage
JSON.stringify / JSON.parseObject
导入 JSON
历史本地数据
页面状态 DTO
```

这些都是动态边界。UTS Android 又是名义类型系统，不能依赖 TypeScript 的结构类型思维。

但为了避免产生大量向后兼容遗留，本次只固化三条项目级规则：

1. 外部 JSON / Storage JSON 不允许直接 `as` 成业务强类型。
2. 历史兼容只允许集中在 parser / adapter / repository 边界，不允许散落到 Page、Store、Validator、业务规则层。
3. 每次只改一个明确边界；发现其他边界问题先记录，除非阻塞当前验收，否则不顺手扩大范围。

---

## 8. 历史兼容收敛策略

### 8.1 不做全量迁移，不制造第二套长期结构

短期不做全量 storage migration，也不新增 `LegacyXXX` 长期并行模型。

推荐做法：

```text
读取旧数据 -> 边界层容错读取/补默认值 -> 输出当前 typed model
保存新数据 -> 只保存当前结构
```

也就是说，兼容逻辑只存在于“读入口”，不会把旧字段继续传播到业务层，也不会在保存时继续写旧结构。

### 8.2 严格区分两类边界

#### 外部导入 JSON：严格模式

导入包是跨设备、跨人传播的数据，应该尽早失败：

```text
缺必填字段 -> preview 失败
字段类型错误 -> preview 失败
重复 id/key -> preview 失败或明确 warning
版本不支持 -> preview 失败并提示升级/重新导出
```

不要为了兼容未知导入包而加入大量猜测逻辑。

#### 本地历史 Storage：容错模式

本地旧数据是用户已经存在的数据，目标是“不崩溃、可打开、可重新保存为新结构”：

```text
缺可选字段 -> 使用默认值
数组字段不是数组 -> 当空数组处理并记录 warning/跳过索引
数字字段缺失 -> 不进入索引或使用业务安全默认值
未知字段 -> 忽略，不回写
```

### 8.3 兼容代码的放置位置

允许兼容逻辑存在于：

```text
TransferService 内部 decoder/parser
session-adapter.uts
storage-repository.uts 的索引提取边界
default-data-parser.uts
AnQuSnapshotService.uts / StageCoefService.uts 的 snapshot parser
```

禁止兼容逻辑散落到：

```text
pages/*.uvue
components/*.uvue
Validator 业务规则主体
Store 的普通状态推导逻辑
```

### 8.4 兼容逻辑退出条件

每个兼容分支都必须有明确用途，不写“永久兜底”。建议注释中说明：

```text
兼容对象：哪个历史字段/哪个历史版本
处理方式：默认值/跳过/转换
退出条件：新保存后不再写旧字段；后续确认无旧数据可删除
```

如果无法说明兼容对象，就不要添加该兼容分支。

---

## 9. 项目级共用边界设计

### 9.1 优先扩展 json-reader，不新增 DynamicBoundary 框架

短期不新建 `utils/dynamic-boundary.uts`。先扩展已有 `utils/json-reader.uts`，只补当前导入修复需要的安全读取能力：

```text
readObjectArray
readStringArray
readNumberArray
isNumber
isString
isObjectArray
safeErrorMessage
```

等两个以上模块稳定复用后，再判断是否需要更通用的 DynamicBoundary 抽象。

### 9.2 模型级工厂函数暂不全量改造

现有 `createFermentRoundFromJSON(raw: UTSJSONObject): FermentRound` 这类函数方向是对的，但不要一次性扩展到所有模型。

短期只改会被导入链路触发、且已经造成崩溃或脏数据风险的模型。其他模型保持现状，等具体模块改造时再补 parser。

### 9.3 Adapter / Repository 边界后置处理

`session-adapter.uts`、`storage-repository.uts` 的边界化有价值，但应作为后续独立任务处理：

```text
一轮只清理一个模块
保留外部函数签名
读取旧数据时容错，保存新数据时只写当前结构
```

不建议在 Transfer 导入 P0 修复中同时抽 `SessionIndexExtractor.uts`。只有当索引提取继续膨胀，或同一逻辑被两个以上模块复用时，再提取独立文件。

---

## 10. 各模块改造优先级

| 优先级 | 模块                                                         | 原因                                             | 建议动作                                               |
| ------ | ------------------------------------------------------------ | ------------------------------------------------ | ------------------------------------------------------ |
| P0     | TransferService 导入解析                                     | 用户正在遇到连续崩溃                             | 补 shape/semantic validation，内部拆函数，外部签名不变 |
| P0     | AppStore import preview/apply                                | preview/apply 有反向 cast、ambiguous、半写入风险 | 只做必要修正，暂不提取 PreviewBuilder / ApplyService   |
| P1     | json-reader                                                  | 多个边界都需要安全数组/异常读取                  | 扩展现有工具，不新增 DynamicBoundary                   |
| P1     | 导入链路触发的历史兼容                                       | roster conflict / round / snapshot 可能碰旧数据  | 兼容集中在 reader/parser，不进入页面和业务规则         |
| P2     | session-adapter                                              | 已多次出现 UTS 类型边界问题                      | 独立任务清理裸数组 cast                                |
| P2     | storage-repository 索引提取                                  | submitted session 遍历会碰历史数据               | 先安全读取；是否抽 Extractor 后置判断                  |
| P3     | default-data-parser / AnQuSnapshotService / StageCoefService | 其他动态边界                                     | 按模块逐步 reader 化                                   |
| 暂缓   | ferment/person/coef 模型工厂全量规范化                       | 范围大、收益依赖具体场景                         | 不做全量模板化，只在触发问题时改                       |

---

## 11. 代码规范建议

### 11.1 禁止写法

```ts
const payload = raw as TransferPayload;
const items = raw["items"] as UTSJSONObject[];
const value = raw["value"] as number;
const obj = error as UTSJSONObject;
const typed = jsonObj as SomeBusinessType;
```

### 11.2 允许写法

在边界 parser/adapter 内允许：

```ts
const rawItems = readObjectArray(raw, "items");
const value = readNumber(raw, "value", 0);
const typed = {
  value: value,
} as SomeBusinessType;
```

严格导入校验中允许先判断再读取：

```ts
const value = raw["value"];
if (typeof value != "number") {
  return error;
}
```

但 builder 里不应重复裸读未校验字段。

---

## 12. 建议测试用例

### 12.1 Transfer 导入

#### CONFIG

- 合法 persons + coef。
- 仅 persons。
- 仅 coef。
- persons 和 coef 同时为空，应失败。
- duplicate person.id，应失败。
- duplicate person.name，应失败或 warning，取决于产品决策。
- coef.stages 重复 code，应失败。
- coef.duiQu 重复 key，应失败。
- coef.liangTang 重复 key，应失败。
- skill 缺失，应失败，因为导入包是新格式；本地历史兼容只在 local conflict 检测处理。

#### ANQ

- 合法 bins。
- bins 为空，应失败。
- bin 缺 `seq`，应失败而非崩溃。
- bin 缺 `floor`，应失败而非崩溃。
- duplicate `id`，应失败。
- duplicate `floor + seq`，应失败。
- duplicate `id` 且 koji 不同，应失败。
- round exact match。
- round soft match。
- round ambiguous，可进入候选选择。
- round not found，阻止导入。

### 12.2 Storage / Session 边界

- session.bins 不是数组，不崩溃。
- workers 不是数组，不崩溃。
- person_id 为空，不进入 person index。
- bin floor/seq 缺失，不进入 bin index。
- 老本地数据缺 `skill_level`，冲突检测不崩溃。

---

## 13. 推荐落地顺序

```text
第 1 步：补 json-reader 缺失数组 reader 与 safeErrorMessage
第 2 步：Transfer 导入 shape/semantic validation 止血
第 3 步：修正 ambiguous preview 交互
第 4 步：收敛 AppStore preview/apply 的反向 cast 与半写入风险
第 5 步：拆分 TransferService.unseal 内部结构，保持外部签名
第 6 步：补充手工样例 / 回归样例，Android 编译验证
第 7 步：独立任务清理 session-adapter 裸数组 cast
第 8 步：独立任务处理 storage-repository 索引提取边界
第 9 步：按需处理 AnQuSnapshotService / StageCoefService / default-data-parser
第 10 步：仅在复用和测试充分时评估 PreviewBuilder / ApplyService / 模型工厂规范化
```

---

## 14. 验证命令

每个阶段至少执行：

```bash
git diff --check -- domain/services/TransferService.uts domain/stores/AppStore.uts pages/transfer/transfer-import.uvue utils/json-reader.uts
./compile-propoints-android.sh cycle
```

涉及 session/storage 边界时执行：

```bash
git diff --check -- domain/services/session-adapter.uts storage/storage-repository.uts utils/json-reader.uts
./compile-propoints-android.sh cycle
```

---

## 15. 最终验收标准

1. 外部导入 JSON 错误只返回业务错误，不再触发 Android runtime crash。
2. 页面和 Store 不再出现 `payload as UTSJSONObject` 这类反向强转。
3. `TransferService` 不再返回 raw JSON 伪装的 typed payload。
4. 导入包内部空数据、重复数据、缺字段数据在 preview 阶段被拦截。
5. ambiguous round 能进入候选选择或产品明确取消该 UI，不再前后矛盾。
6. session/storage/默认人员/快照等动态边界逐步复用同一 reader + builder 规则。
7. Android 编译持续通过。

---

## 16. 一句话原则

> 在 UTS Android 项目中，`as Type` 只能告诉编译器，不会把运行时对象变成真实 Type；任何来自 JSON 或 Storage 的数据，都必须经过边界层逐字段读取、校验和构造。
