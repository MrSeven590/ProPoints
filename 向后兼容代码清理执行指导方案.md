# Task/Feature: ProPoints 向后兼容代码清理执行指导方案

## Overview

本方案用于指导 ProPoints 项目在“尚未投入使用、不需要旧数据向后兼容”的前提下，清理此前强类型化改造遗留的兼容入口、别名、fallback 与过渡注释，使代码表达与当前业务阶段一致。

本方案按可执行任务文档组织，可供 goal 命令 / Plan Agent 消费：每项任务均给出目标文件、动作、验收标准、风险与回滚建议。执行时必须小步提交、逐步验证；不得 `git push`。

## Goal

1. 删除已无必要的历史兼容入口和别名，让新代码只依赖 typed 主路径。
2. 移除项目未上线阶段不需要的旧字段 fallback，降低字段双轨和误用风险。
3. 保留必要的边界层转换、审计字段与开发重置能力，避免把“清理兼容”误做成破坏性重构。
4. 保持每一步可编译、可验证、可局部回滚。

## Context / References

只读调研参考：

- `AGENTS.md`：项目使用 Trellis 体系，任务文档应结构清晰、边界明确、可验证。
- `PHASE2_IMPLEMENTATION_GUIDE.md`：历史 Phase 2 强类型化计划；当前仅作为背景参考，不再按旧 checklist 重跑。
- `A-G架构重复问题收口推进计划.md`：当前 A-G 收口判断，核心链路已基本 typed，重点转为清理过渡层。
- `domain/models/session.uts`
- `domain/services/Validator.uts`
- `domain/services/session-adapter.uts`
- `domain/services/TransferService.uts`
- `domain/models/transfer.uts`
- `domain/stores/AppStore.uts`
- `pages/mine/coeff-config.uvue`
- `pages/work/entry.uvue`

关键现状：

- `Validator.uts` 已有 typed 主入口 `validateTypedSessionData(data: SessionData, ctx)`，旧入口 `validateSessionData(raw, ctx)` 只是 wrapper。
- `domain/models/session.uts` 中 `EntryPageState` 是真实页面状态类型，`SessionEntryPageState` 仅为 deprecated 别名。
- `session-adapter.uts` 仍保留大量 snake_case / camelCase 双读 fallback，例如 `person_id/personId`、`points_units/pointsUnits`、`stage_name/stageName`、`total_koji_count/totalKojiCount`。
- `TransferService.uts` 导出侧仍从 `roundInfo['micro_enabled']` fallback 到旧 `roundInfo['micro']`；导入轮次解析侧仍读取 `round['micro']`。
- `saveCoefSetIfChanged(remark, coefSet)` 的 `remark` 参数未使用，调用方传空字符串。
- entry 页面 wrapper/容器类仍应保留，当前清理目标是注释语义，不是 UI 结构删除。
- 审计字段 `created_by_manager` / `updated_by_manager`、页面态 `createdByManager` / `updatedByManager` 已存在；后续应补齐缺口而不是删除。
- `resetSessionStorage` 未在本轮定位到直接匹配项；即使存在，也不建议删除，应保留为开发/调试/重置能力或仅收窄暴露范围。

## Scope

本次清理范围限定为：

1. `Validator` 旧 raw 校验入口及其专用标准化 wrapper。
2. `SessionEntryPageState` deprecated 类型别名。
3. `saveCoefSetIfChanged` 未使用的 `remark` 参数。
4. `session-adapter` 中读取 storage DTO 时的 camelCase 旧字段 fallback。
5. `TransferService` 中本地轮次旧字段 `micro` fallback。
6. entry/核心组件 wrapper 的注释语义清理，不删除结构。
7. 审计字段补齐检查。
8. reset 类能力审计但不删除。

## Out of Scope

- 不做全项目 `UTSJSONObject` 清零。
- 不重构 storage repository、导入导出整体协议、统计报表、人员管理等非核心范围。
- 不改 PPX1 外部传输字段名 `m`。
- 不删除审计字段。
- 不删除 reset/resetAllStorage/resetSessionStorage 类能力。
- 不做 UI 样式重构。
- 不引入新数据库/云同步/历史迁移逻辑。
- 不执行 `git push`。

## Requirements

### R1. 删除 Validator 旧入口

目标文件：

- `domain/services/Validator.uts`
- 所有引用 `validateSessionData` 的调用方

要求：

- 删除或停止导出 `validateSessionData(raw: UTSJSONObject, ctx)`。
- 删除仅为旧入口服务的 `normalizeSessionData(raw: UTSJSONObject)`。
- 保留 `validateTypedSessionData(data: SessionData, ctx)` 作为唯一校验入口。
- 更新文件顶部注释中 “validateSessionData() 是纯函数” 的旧表述，改为 `validateTypedSessionData()`。
- 全项目搜索确认业务调用方不再 import/use `validateSessionData`。

### R2. 删除 `SessionEntryPageState` 别名

目标文件：

- `domain/models/session.uts`
- 所有 import/use `SessionEntryPageState` 的文件，重点 `pages/work/entry.uvue`、`domain/services/session-adapter.uts` 如有引用

要求：

- 删除：
  - `export type SessionEntryPageState = EntryPageState`
  - 相关 deprecated 注释。
- 全部改为直接使用 `EntryPageState`。
- 不改变 `EntryPageState` 字段结构。

### R3. 删除 `saveCoefSetIfChanged` 的 `remark` 参数

目标文件：

- `domain/stores/AppStore.uts`
- `pages/mine/coeff-config.uvue`

现状：

- `saveCoefSetIfChanged(remark: string, coefSet: UTSJSONObject)` 中 `remark` 未使用。
- 调用方为 `saveCoefSetIfChanged('', coefData)`。

要求：

- 函数签名改为 `saveCoefSetIfChanged(coefSet: UTSJSONObject): UTSJSONObject`。
- 删除 JSDoc 中“备注（未使用，保留兼容性）”。
- 调用方改为 `saveCoefSetIfChanged(coefData)`。
- 不改变系数比较、版本生成、保存 active set 的业务逻辑。
- 不改变返回结构 `{ changed: boolean, setId: number }`。

### R4. 简化 `session-adapter` camelCase fallback

目标文件：

- `domain/services/session-adapter.uts`

要求：

- Storage DTO 读取只保留当前正式 snake_case 字段。
- 删除页面态/历史态 camelCase fallback：
  - worker 字段：`personId`、`personName`、`pointsUnits`、`deductedUnits`、`penaltyReason`、`penaltyId`、`positionIndex`。
  - cross-bin source 字段：`assignmentId`、`stageBinId`、`sourcePointsUnits`。
  - session 字段：`stageName`。
  - dui_qu 字段：`totalKojiCount`、`totalPoolUnits`。
  - 其他同类 camelCase fallback 需一并搜索清理。
- 保留 snake_case storage DTO 与 camelCase page state 的正常转换：
  - `storageJsonToSessionData`：只读 snake_case storage。
  - `sessionDataToEntryPageState`：输出 camelCase 页面态。
  - `entryPageStateToSessionData`：从 camelCase 页面态生成 snake_case `SessionData`。
- 不删除 null/default 保护：空数组、空字符串、0、null 等默认值仍需保留。

### R5. 移除 `TransferService` micro 旧字段 fallback

目标文件：

- `domain/services/TransferService.uts`
- 必要时同步 `domain/models/transfer.uts` 注释命名

现状：

- 导出侧 `collectAnQuPayload` 使用：`micro_enabled` fallback 到 `micro`。
- 导入轮次解析 `resolveRound` 读取 `round['micro']`。
- `AnQuPayload.m` 是传输字段，仍需保留；问题是本地 FermentRound 旧字段 fallback。

要求：

- 本地轮次对象只读取 `micro_enabled`。
- 删除 `roundInfo['micro']` fallback。
- `resolveRound` 遍历本地 `allRounds` 时改读 `round['micro_enabled']`，不再读 `round['micro']`。
- `AnQuPayload.m` 作为 PPX1 传输协议字段保留，不在本轮重命名；可补注释说明 `m` 对应本地 `micro_enabled`。
- 调整 warning/label 文案时避免扩大语义变更；可继续显示“微机计分权”或沿用现有“微调版本”，但建议后续单独统一术语。

### R6. entry wrapper 保留但改注释

目标文件：

- `pages/work/entry.uvue`
- 如涉及组件 wrapper：`components/biz-bin-card/biz-bin-card.uvue`、`components/biz-session-role-card/biz-session-role-card.uvue`

要求：

- 不删除页面/组件中的 wrapper `<view>` 结构和 CSS 类；这些多为布局、滚动、列表、样式挂载点。
- 仅将过渡期、历史兼容、legacy 语义的注释改为当前结构说明。
- 注释应表达“布局容器 / 组件边界 / typed 数据流”，避免继续暗示旧兼容路径仍是主路径。
- 不改变模板结构、不改 class 名、不改样式逻辑，除非编译明确要求。

### R7. 审计字段补齐，不删除

目标文件：

- `domain/models/session.uts`
- `domain/services/session-adapter.uts`
- `domain/stores/AppStore.uts`
- `pages/work/entry.uvue`
- 其他保存/导入/导出链路中构造 session/assignment 的位置

要求：

- 保留并检查 `created_by_manager`、`updated_by_manager`、`createdByManager`、`updatedByManager`。
- 新建数据时：`created_by_manager` 应来自当前管理员；无管理员时保留空字符串但不得抛异常。
- 更新数据时：`created_by_manager` 保留原值；`updated_by_manager` 使用当前管理员，当前管理员为空时保留原 updated 值。
- 导入/默认/测试构造如缺字段，应补默认值，而不是删除字段。
- 验收时搜索确认 `SessionData` 与 storage JSON 输出仍包含审计字段。

### R8. `resetSessionStorage` 不建议删除

目标文件：

- 若存在：包含 `resetSessionStorage` 或同类 reset storage 能力的文件。
- 已知相关：`domain/stores/AppStore.uts` 引入 `resetAllStorage`。

要求：

- 不把 reset 能力纳入本轮删除范围。
- 若发现对生产 UI 暴露过宽，可改为开发入口、确认弹窗、或仅调整注释；不要直接移除底层函数。
- 原因：项目虽未投入使用，但开发/验收阶段仍需要一键清理本地存储以验证新 schema 与初始态。

## Technical Notes

1. 项目未上线，因此本轮可以删除旧字段兼容逻辑；但仍要保留当前正式 schema 的 null/default 防御。
2. `session-adapter` 的责任仍是边界转换，不应因为删除 fallback 而把 snake_case 泄漏到页面层。
3. 删除 `validateSessionData` 前必须全局搜索引用；如仍有导入/调用，应先改为 `validateTypedSessionData` 并确保调用方传入 `SessionData`。
4. `saveDraftSession` / `saveSubmittedSession` 当前公开版本已接收 `SessionData` 并内部 `sessionDataToStorageJson`，不要回退为 raw JSON 调用。
5. `saveCoefSetIfChanged` 的返回值 `{ changed, setId }` 仍被 UI 使用，不要改变返回结构。
6. Transfer 中 `payload.m` 与本地 `round.micro_enabled` 是两个层级：传输压缩字段可保留，本地旧字段 fallback 要删除。

## Execution Plan / Task Breakdown

### Step 0. 建立基线与搜索清单

动作：

- 搜索：
  - `validateSessionData`
  - `normalizeSessionData`
  - `SessionEntryPageState`
  - `saveCoefSetIfChanged`
  - `personId` / `pointsUnits` 在 `session-adapter.uts` 的 fallback 使用
  - `round['micro']` / `roundInfo['micro']`
  - `created_by_manager` / `updated_by_manager`
  - `resetSessionStorage` / `resetAllStorage`

验收：

- 得到待改引用列表。
- 明确每个引用属于“删除目标”“保留目标”还是“注释调整”。

### Step 1. 清理 Validator 旧入口

动作：

1. 改 `Validator.uts` 顶部设计原则注释为 `validateTypedSessionData()`。
2. 删除 `normalizeSessionData(raw)`。
3. 删除 `validateSessionData(raw, ctx)` export。
4. 修正 imports：如果 `storageJsonToSessionData` 不再使用，从 `Validator.uts` 删除 import。
5. 更新所有调用方到 `validateTypedSessionData`。

验证：

- `validateSessionData` 全项目搜索无业务引用。
- `validateTypedSessionData` 仍存在并被 entry 提交流程使用。
- 编译通过。

### Step 2. 删除 `SessionEntryPageState` 别名

动作：

1. 所有 import/use 替换为 `EntryPageState`。
2. 删除 `domain/models/session.uts` 中 deprecated alias。
3. 检查 `session-adapter` 函数签名维持 `EntryPageState`。

验证：

- `SessionEntryPageState` 全项目搜索为 0。
- `EntryPageState` 编译通过。

### Step 3. 删除 `saveCoefSetIfChanged` remark 参数

动作：

1. `AppStore.uts` 函数签名改为 `saveCoefSetIfChanged(coefSet: UTSJSONObject)`。
2. 删除 JSDoc remark 说明。
3. `pages/mine/coeff-config.uvue` 调用改为 `saveCoefSetIfChanged(coefData)`。

验证：

- `saveCoefSetIfChanged('',` 搜索为 0。
- 系数保存 UI 提示逻辑仍依赖 `result.changed` / `result.setId`。

### Step 4. 简化 `session-adapter` fallback

动作：

1. 将 `readNumberField/readStringField/readNullableNumberField` 改为只接收 snakeKey，或直接改为 `readNumberField(source, key, defaultValue)`。
2. 删除 worker camelCase fallback helper 中的 camelKey 参数。
3. `parseCrossBin` sources 只读 `assignment_id/stage_bin_id/source_points_units`。
4. `storageJsonToSessionData` 只读 `stage_name`。
5. `parseDuiQu` 只读 `total_koji_count/total_pool_units`。
6. 保持 `sessionDataToEntryPageState` 和 `entryPageStateToSessionData` 的 camelCase 页面态转换不变。

验证：

- `session-adapter.uts` 中不再出现 `personId`、`pointsUnits` 等 camelCase fallback 字符串，除正常 page state 输出/输入映射外。
- 保存后的 storage JSON 仍是 snake_case。
- 新建/加载/保存/重载至少人工验证一次。

### Step 5. 移除 TransferService micro 旧字段 fallback

动作：

1. `collectAnQuPayload` 改为只读 `roundInfo['micro_enabled']`，默认值仍可为 0。
2. `resolveRound` 改为只读本地 round 的 `micro_enabled`。
3. 保留 `payload.m`。
4. 视情况更新注释：`m` 是传输压缩字段，对应本地 `micro_enabled`。

验证：

- `roundInfo['micro']` / `round['micro']` 搜索为 0。
- 导出 ANQ/BUNDLE payload 仍包含 `m`。
- 导入轮次匹配仍按 class/year/round/micro_enabled 工作。

### Step 6. entry wrapper 注释调整

动作：

1. 搜索 `entry.uvue` 与核心组件内 legacy/兼容/过渡注释。
2. 对 wrapper 结构只改注释，不改模板层级。
3. 将注释改为布局语义，如“页面滚动容器”“仓卡片列表容器”“岗位人员项容器”。

验证：

- 模板结构 diff 极小，仅注释变化。
- entry 页面编译/预览不受影响。

### Step 7. 审计字段补齐检查

动作：

1. 检查所有构造 `SessionData` 的位置是否包含：
   - `created_by_manager`
   - `updated_by_manager`
   - `timestamp`
2. 检查 `entryPageStateToSessionData` 仍保持：
   - created 优先保留 state 原值；为空时使用当前 manager。
   - updated 优先使用当前 manager；为空时保留 state 原值。
3. 检查导入/测试/默认数据路径缺字段时是否有默认值。

验证：

- `sessionDataToStorageJson` 输出包含审计字段。
- 新建保存后 storage 中有审计字段。
- 修改保存后 `created_by_manager` 不被误覆盖。

### Step 8. reset 能力审计但不删除

动作：

1. 搜索 `resetSessionStorage`、`resetAllStorage`、`resetAllStorageFromInit`。
2. 若仅为开发/设置页确认清理入口，保留。
3. 若注释暗示生产迁移兼容，可改注释为“开发/验收重置”。

验证：

- reset 能力仍可用于开发环境清理本地数据。
- 没有将 reset 纳入兼容清理删除项。

## Suggested Execution Order

1. 基线搜索与引用列表确认。
2. 删除 `saveCoefSetIfChanged` remark 参数（低风险、范围小）。
3. 删除 `SessionEntryPageState` 别名（类型命名清理，容易编译发现遗漏）。
4. 删除 Validator 旧入口（核心但边界清晰）。
5. 简化 `session-adapter` camelCase fallback（风险最高，需紧跟编译和手工回归）。
6. 移除 TransferService 本地 `micro` fallback（导入导出专项验证）。
7. entry wrapper 注释调整（低风险，避免混入结构改动）。
8. 审计字段补齐与 reset 能力审计（最后做防误删确认）。
9. 全量编译与人工回归。

## Acceptance Criteria

- [ ] `validateSessionData` 和 `normalizeSessionData` 不再作为导出/业务入口存在。
- [ ] `validateTypedSessionData` 是唯一会话校验主入口。
- [ ] `SessionEntryPageState` 全项目搜索为 0，统一使用 `EntryPageState`。
- [ ] `saveCoefSetIfChanged` 只有 `coefSet` 参数，调用方不再传 `''` remark。
- [ ] `session-adapter.uts` 不再对 storage DTO 读取 camelCase 旧字段 fallback。
- [ ] `TransferService.uts` 不再读取本地旧字段 `micro`，只读取 `micro_enabled`；传输 payload 字段 `m` 保留。
- [ ] entry/core component wrapper 结构未删除，仅必要注释更新。
- [ ] `created_by_manager` / `updated_by_manager` 在模型、adapter 输出、保存链路中保留并补齐默认值。
- [ ] reset/resetAllStorage/resetSessionStorage 类能力未被删除。
- [ ] 编译通过。
- [ ] 新建会话、保存草稿、提交、重新加载、日期切换、轮次切换核心路径正常。
- [ ] 不执行 `git push`。

## Verification / DoD

### Static Search

执行后建议搜索：

```text
validateSessionData
normalizeSessionData
SessionEntryPageState
saveCoefSetIfChanged('',
roundInfo['micro']
round['micro']
personId
pointsUnits
stageName
totalKojiCount
totalPoolUnits
created_by_manager
updated_by_manager
resetSessionStorage
resetAllStorage
```

判定：

- 删除目标搜索应为 0 或仅出现在历史 Markdown 中。
- `personId/pointsUnits` 在组件/page state 映射中可存在；在 storage DTO fallback 中不应存在。
- `created_by_manager/updated_by_manager` 必须存在。
- reset 相关能力允许存在。

### Build / Compile

优先执行项目实际可用命令：

```bash
./compile-propoints-android.sh cycle
```

如当前环境无法完整编译，至少记录原因，并执行可用的类型/静态检查。

### Manual Regression

- [ ] 新建 entry 页面打开正常。
- [ ] 已保存 draft 回填正常。
- [ ] submitted 只读显示正常。
- [ ] 仓卡片人员、曲量、扣分编辑正常。
- [ ] 晾堂岗位含微机启用/禁用显示正常。
- [ ] 堆曲岗位编辑正常。
- [ ] 跨仓人员、工分、来源预览正常。
- [ ] 自动保存正常。
- [ ] 提交校验正常。
- [ ] 保存后重新进入数据一致。
- [ ] 系数页保存仍能区分“有变更/未变更”。
- [ ] ANQ/BUNDLE 导出导入轮次匹配正常。

## Risks / Rollback

### Risk 1. 删除 Validator 旧入口后仍有调用方

信号：编译报 `validateSessionData` import/export 不存在。

回滚/止损：

- 优先改调用方为 `validateTypedSessionData`。
- 若短期无法改完，可临时恢复 wrapper，但标记为阻塞项，不进入完成状态。

### Risk 2. session-adapter fallback 删除过多导致默认数据加载失败

信号：新建/加载时报字段 null、页面空白、保存后重载字段丢失。

回滚/止损：

- 只恢复必要的 null/default 防御，不恢复 camelCase 旧字段 fallback。
- 确认输入源是否确实为当前 snake_case storage。

### Risk 3. Transfer micro 字段混淆

信号：导入轮次无法匹配或 warning 异常。

回滚/止损：

- 检查本地 rounds 是否统一保存 `micro_enabled`。
- 保留 payload `m` 不变；只修本地读取字段。

### Risk 4. 误删 wrapper 影响 UI

信号：页面布局错乱、列表样式失效。

回滚/止损：

- wrapper 结构不应删除；若已删除，恢复模板和 CSS class，只保留注释调整。

### Risk 5. 审计字段被误删

信号：保存后 storage 缺少 `created_by_manager/updated_by_manager`，或 created 在编辑后被覆盖。

回滚/止损：

- 恢复 `SessionData` 字段与 `entryPageStateToSessionData` 的 created/updated 逻辑。
- 补默认值，不做字段删除。

### Risk 6. reset 能力误删影响验收

信号：无法快速清空本地数据验证新 schema。

回滚/止损：

- 恢复 reset 函数；如担心误触，在 UI 层加确认或隐藏到开发入口。

## Open Questions

1. `TransferService` 中 “micro / 微调版本” 文案是否应统一改为“微机计分权”？本方案建议本轮只删除本地旧字段 fallback，术语统一可作为后续小任务。
2. reset 类能力是否需要隐藏到开发入口？本方案只要求不删除；是否收窄 UI 暴露需由产品/开发负责人确认。
3. 历史 Markdown 是否需要同步删除旧兼容描述？本方案不要求修改历史文档，除非团队决定做文档归档清理。
4. 若 goal 执行期间发现新的旧字段 fallback，是否纳入本轮？建议只纳入同一文件同一语义的 fallback；跨模块新增发现应先记录再单独评估。

## Goal 命令使用建议

建议向 goal 命令提供本文件并明确以下约束：

```text
请按《向后兼容代码清理执行指导方案.md》执行。项目尚未投入使用，不需要旧数据向后兼容。只清理方案范围内的兼容入口、别名、fallback 和注释；不要删除审计字段，不要删除 reset 能力，不要改 PPX1 payload.m，不要做 UI 重构，不要 git push。每完成一个步骤先编译/搜索验证，再进入下一步。
```

执行期建议：

- 先让 goal/Plan Agent 输出具体改动计划和文件列表，再开始修改。
- 每个 Step 尽量单独 commit 或至少单独验证。
- `session-adapter` fallback 清理风险最高，应安排在类型别名和小参数清理之后。
- 如果编译失败，优先修当前 Step，不要继续扩散到下一 Step。

## Non-Goals / Guardrails

- 不追求一次性清空所有历史 Markdown 中的旧提法。
- 不在清理任务中扩展业务功能。
- 不为旧数据写迁移脚本。
- 不引入新的兼容 wrapper 替代旧 wrapper。
- 不删除 reset 能力。
- 不删除审计字段。
- 不 git push。
