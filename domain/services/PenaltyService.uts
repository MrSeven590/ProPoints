/**
 * @deprecated 旧扣分服务已下线。
 *
 * 扣分唯一事实源为 worker 内联字段：
 * - deducted_units / deductedUnits
 * - penalty_reason / penaltyReason
 * - penalty_id / penaltyId
 *
 * 旧版 PenaltyService 曾维护 sessionData['penalties'] 独立明细口径，并通过 StorageUnsafe
 * 直接改写 session JSON，存在扣分双口径和绕过索引/适配器约束风险。
 *
 * 业务代码禁止从本文件导入任何扣分 CRUD/查询能力；新增扣分逻辑请使用
 * PenaltyDraftService + session-adapter 的 worker 内联字段链路。
 */
